import { StyleSheet } from "react-native";

const Theme = StyleSheet.create({
  colors:{
    dark:{
      primary:'#e76b74',
      onSurface: '#ffffff',
      surface :'#585b56',
    }
  },
});

export default Theme