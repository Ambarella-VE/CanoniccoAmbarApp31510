import React from 'react';
import {
  View, 
  StyleSheet,
  Text
} from 'react-native';

const AppShell = () => {
  return (
    <View>
      <Text>AppShell</Text>
    </View>
  );
}

const styles = StyleSheet.create({})

export default AppShell;
